# Leaderboard

Interactive real-time classroom leaderboard web app built with plain HTML, CSS, and JavaScript.

## Run locally

Open `index.html` in a browser, or serve the project directory with a simple static server such as:

```bash
cd /path/to/Leaderboard
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
